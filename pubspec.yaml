name: go_transitions
description: "Easily create, combine and apply transitions to routes with GoTransitions."
version: 0.8.3
homepage: https://www.branvier.com
repository: https://github.com/arthurbcd/go_transitions.git
publish_to: none
topics:
 - go-router
 - page
 - transition
 - transitions

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  go_router:
    git: https://github.com/kumsumit/go_router.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
